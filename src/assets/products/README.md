# products

Product photography, one folder per product slug (e.g. `southern-polo/`). Wire paths into the `images` array in `src/data/products.ts`; until then the drawn garments stand in automatically.

See ../README.md for naming, format and art direction.
